#include<stdio.h>
#include<string.h>
int main()
{
	char s[20];
	int cnt;
	printf("enter the name");
	scanf("%s",s);
	//for(int i=0;i<=strlen;i++)
	for(int i=0,cnt=0;str[i]!='\0';i++)
	{
		if(str[i]=='a'
			
	return 0;
}                                                             